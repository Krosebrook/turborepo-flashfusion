/**
 * Logger for AI Agents package
 * Reuses the shared universal logger
 */

// Use the shared universal logger
const universalLogger = require('../../shared/utils/universalLogger');

module.exports = universalLogger;